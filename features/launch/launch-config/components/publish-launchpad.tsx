import Button from "@/components/buttons/Button";

export default function PublishLaunchPad() {
  return (
    <div className="p-10">
      <div>Review your launchpad before publishing</div>
      <Button className="mt-5">Preview Launchpad</Button>
    </div>
  );
}
